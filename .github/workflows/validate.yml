name: validate

# The catalog's half of the matching contract. An artifact whose tags leave the
# vocabulary does not fail loudly - it simply never gets selected, and the author
# has no way to know. This workflow is where that silence becomes a red build.
on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    # ubuntu-latest is sufficient even though half of what is checked here is
    # Windows behaviour. The id collision check, and the TASK-001 path rules
    # beside it, model Windows and default macOS filesystems by folding names
    # explicitly rather than by observing the runner's own filesystem - which is
    # also the only way to check them at all, since 'CON.md' and 'trailing.'
    # cannot exist on a Windows runner either.
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Pester
        shell: pwsh
        run: |
          Install-Module -Name Pester -RequiredVersion 5.6.1 -Force -Scope CurrentUser -SkipPublisherCheck

      - name: Validate the catalog index
        shell: pwsh
        run: |
          # -CatalogRoot turns on the path rules over every file in the
          # checkout. Without it the index is validated and the tree is not,
          # which is the state this workflow was in before TASK-001's rules
          # were enforced anywhere.
          $result = ./scripts/validate-catalog.ps1 -IndexPath ./index.json -CatalogRoot .

          Write-Host "Validated $($result.ArtifactCount) artifacts and $($result.PathCount) checkout paths."

          if (-not $result.IsValid) {
              Write-Host "::error::The catalog index failed validation with $(@($result.Errors).Count) error(s)."
              foreach ($message in @($result.Errors)) {
                  Write-Host "::error::$message"
              }
              exit 1
          }

          Write-Host 'Catalog index is valid.'

      - name: Run the validator test suite
        shell: pwsh
        run: |
          Import-Module Pester -RequiredVersion 5.6.1

          $configuration = New-PesterConfiguration
          $configuration.Run.Path = './tests'
          $configuration.Run.PassThru = $true
          $configuration.Output.Verbosity = 'Detailed'

          $result = Invoke-Pester -Configuration $configuration

          # Checked explicitly rather than trusting the exit code: Invoke-Pester
          # with PassThru returns an object and does not itself fail the step.
          if ($result.FailedCount -gt 0) {
              Write-Host "::error::$($result.FailedCount) test(s) failed."
              exit 1
          }
